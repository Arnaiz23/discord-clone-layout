{
  "name": "discord-clone",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "stylelint": "npx stylelint '**/*.css' --fix",
    "deploy2": "netlify deploy  --site discord-arnaizdev --prod",
    "deploy": "sh ./deploy.sh"
  },
  "devDependencies": {
    "stylelint": "^15.10.3",
    "stylelint-config-manzdev": "^0.0.10",
    "vite": "^4.4.5",
    "netlify-cli": "^16.4.1"
  }
}
